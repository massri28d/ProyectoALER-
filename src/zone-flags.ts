/**
 * Zone flags para jeep-sqlite
 * Este archivo debe importarse ANTES de zone.js en polyfills.ts
 */

// Deshabilita el patch de customElements para evitar conflictos con Stencil (jeep-sqlite)
(window as any).__Zone_disable_customElements = true;
